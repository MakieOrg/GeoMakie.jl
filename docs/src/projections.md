```@raw html
<!-- AUTO-GENERATED by docs/generate_projections.jl. Do not edit by hand;
     edit PROJECTION_SECTIONS in that script and rebuild the docs. -->
```

# Projections

A `GeoAxis` can draw (almost) every projection PROJ provides. Each section below shows the
PROJ string for a projection and the code that draws it, with related variants grouped into
tabs.

Every panel is the same few lines with a different `dest`. Copy this preamble once, then any
panel below reproduces on its own:

```@example projections
using GeoMakie, CairoMakie
CairoMakie.activate!(type = :svg)

land = GeoMakie.land()
nothing # hide
```

## Adams projections

::: tabs

== Hemisphere in a square

````@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=adams_hemi", title = "+proj=adams_hemi",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
````

== World in a square I

````@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=adams_ws1", title = "+proj=adams_ws1",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
````

== World in a square II

````@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=adams_ws2", title = "+proj=adams_ws2",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
````

:::

## Albers Equal-Area Conic

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=aea +lat_1=29.5 +lat_2=42.5", title = "+proj=aea +lat_1=29.5 +lat_2=42.5",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Azimuthal Equidistant

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=aeqd", title = "+proj=aeqd",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Airy

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=airy", title = "+proj=airy",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Aitoff

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=aitoff", title = "+proj=aitoff",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Apian Globular I

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=apian", title = "+proj=apian",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## August Epicycloidal

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=august", title = "+proj=august",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Bacon Globular

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=bacon", title = "+proj=bacon",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Bertin 1953

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=bertin1953", title = "+proj=bertin1953",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Bipolar Conic

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=bipc +ns", title = "+proj=bipc +ns",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Boggs Eumorphic

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=boggs", title = "+proj=boggs",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Bonne

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=bonne +lat_1=10", title = "+proj=bonne +lat_1=10",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Cylindrical Equal-Area

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=cea", title = "+proj=cea",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Chamberlin Trimetric

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=chamb +lat_1=10 +lon_1=30 +lon_2=40", title = "+proj=chamb +lat_1=10 +lon_1=30 +lon_2=40",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Collignon

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=collg", title = "+proj=collg",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Compact Miller

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=comill", title = "+proj=comill",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Craster Parabolic

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=crast", title = "+proj=crast",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Denoyer Semi-Elliptical

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=denoy", title = "+proj=denoy",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Eckert

::: tabs

== I

````@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=eck1", title = "+proj=eck1",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
````

== II

````@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=eck2", title = "+proj=eck2",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
````

== III

````@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=eck3", title = "+proj=eck3",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
````

== IV

````@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=eck4", title = "+proj=eck4",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
````

== V

````@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=eck5", title = "+proj=eck5",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
````

== VI

````@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=eck6", title = "+proj=eck6",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
````

:::

## Equidistant Cylindrical (Plate Carrée)

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=eqc", title = "+proj=eqc",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Equidistant Conic

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=eqdc +lat_1=55 +lat_2=60", title = "+proj=eqdc +lat_1=55 +lat_2=60",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Equal Earth

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=eqearth", title = "+proj=eqearth",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Euler

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=euler +lat_1=67 +lat_2=75", title = "+proj=euler +lat_1=67 +lat_2=75",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Fahey

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=fahey", title = "+proj=fahey",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Foucaut projections

::: tabs

== Foucaut

````@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=fouc", title = "+proj=fouc",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
````

== Foucaut Sinusoidal

````@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=fouc_s", title = "+proj=fouc_s",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
````

:::

## Gall Stereographic

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=gall", title = "+proj=gall",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Geostationary Satellite

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=geos +h=35785831.0 +lon_0=-60 +sweep=y", title = "+proj=geos +h=35785831.0 +lon_0=-60 +sweep=y",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Ginsburg VIII

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=gins8", title = "+proj=gins8",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## General Sinusoidal Series

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=gn_sinu +m=2 +n=3", title = "+proj=gn_sinu +m=2 +n=3",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Goode Homolosine

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=goode", title = "+proj=goode",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Guyou

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=guyou", title = "+proj=guyou",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Hammer

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=hammer", title = "+proj=hammer",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Hatano Asymmetrical Equal-Area

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=hatano", title = "+proj=hatano",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Interrupted projections

::: tabs

== Goode Homolosine

````@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=igh", title = "+proj=igh",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
````

== Goode Homolosine (oceanic)

````@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=igh_o +lon_0=-160", title = "+proj=igh_o +lon_0=-160",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
````

== Mollweide

````@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=imoll", title = "+proj=imoll",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
````

== Mollweide (oceanic)

````@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=imoll_o +lon_0=-160", title = "+proj=imoll_o +lon_0=-160",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
````

:::

## International Map of the World Polyconic

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=imw_p +lat_1=30 +lat_2=-40", title = "+proj=imw_p +lat_1=30 +lat_2=-40",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Icosahedral Snyder Equal-Area

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=isea", title = "+proj=isea",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Kavrayskiy

::: tabs

== V

````@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=kav5", title = "+proj=kav5",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
````

== VII

````@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=kav7", title = "+proj=kav7",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
````

:::

## Lambert Azimuthal Equal-Area

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=laea", title = "+proj=laea",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Lagrange

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=lagrng", title = "+proj=lagrng",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Larrivée

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=larr", title = "+proj=larr",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Laskowski

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=lask", title = "+proj=lask",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Lambert Conformal Conic (Alternative)

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=lcca +lat_0=35", title = "+proj=lcca +lat_0=35",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Lambert Equal-Area Conic

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=leac", title = "+proj=leac",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Loximuthal

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=loxim", title = "+proj=loxim",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Space Oblique (Landsat)

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=lsat +ellps=GRS80 +lat_1=-60 +lat_2=60 +lsat=2 +path=2", title = "+proj=lsat +ellps=GRS80 +lat_1=-60 +lat_2=60 +lsat=2 +path=2",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## McBryde–Thomas projections

::: tabs

== Flat-Polar Sine (No. 1)

````@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=mbt_s", title = "+proj=mbt_s",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
````

== Flat-Pole Sine (No. 2)

````@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=mbt_fps", title = "+proj=mbt_fps",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
````

== Flat-Polar Parabolic

````@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=mbtfpp", title = "+proj=mbtfpp",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
````

== Flat-Polar Quartic

````@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=mbtfpq", title = "+proj=mbtfpq",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
````

== Flat-Polar Sinusoidal

````@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=mbtfps", title = "+proj=mbtfps",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
````

:::

## Mercator

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=merc", title = "+proj=merc",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Miller Cylindrical

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=mill", title = "+proj=mill",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Mollweide

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=moll", title = "+proj=moll",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Murdoch

::: tabs

== I

````@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=murd1 +lat_1=30 +lat_2=50", title = "+proj=murd1 +lat_1=30 +lat_2=50",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
````

== III

````@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=murd3 +lat_1=30 +lat_2=50", title = "+proj=murd3 +lat_1=30 +lat_2=50",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
````

:::

## Natural Earth

::: tabs

== I

````@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=natearth", title = "+proj=natearth",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
````

== II

````@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=natearth2", title = "+proj=natearth2",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
````

:::

## Nell projections

::: tabs

== Nell

````@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=nell", title = "+proj=nell",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
````

== Nell–Hammer

````@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=nell_h", title = "+proj=nell_h",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
````

:::

## Nicolosi Globular

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=nicol", title = "+proj=nicol",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## General Oblique Transformation

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=ob_tran +o_proj=mill +o_lon_p=40 +o_lat_p=50 +lon_0=60", title = "+proj=ob_tran +o_proj=mill +o_lon_p=40 +o_lat_p=50 +lon_0=60",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Oblique Cylindrical Equal-Area

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=ocea", title = "+proj=ocea",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Oblated Equal-Area

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=oea +m=1 +n=2", title = "+proj=oea +m=1 +n=2",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Oblique Mercator

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=omerc +lat_1=45 +lat_2=55", title = "+proj=omerc +lat_1=45 +lat_2=55",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Ortelius Oval

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=ortel", title = "+proj=ortel",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Orthographic

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=ortho", title = "+proj=ortho",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Patterson Cylindrical

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=patterson", title = "+proj=patterson",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## American Polyconic

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=poly", title = "+proj=poly",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Putniņš

::: tabs

== P1

````@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=putp1", title = "+proj=putp1",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
````

== P2

````@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=putp2", title = "+proj=putp2",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
````

== P3

````@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=putp3", title = "+proj=putp3",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
````

== P3′

````@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=putp3p", title = "+proj=putp3p",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
````

== P4′

````@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=putp4p", title = "+proj=putp4p",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
````

== P5

````@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=putp5", title = "+proj=putp5",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
````

== P5′

````@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=putp5p", title = "+proj=putp5p",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
````

== P6

````@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=putp6", title = "+proj=putp6",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
````

== P6′

````@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=putp6p", title = "+proj=putp6p",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
````

:::

## Quartic Authalic

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=qua_aut", title = "+proj=qua_aut",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Robinson

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=robin", title = "+proj=robin",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Roussilhe Stereographic

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=rouss", title = "+proj=rouss",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Rectangular Polyconic

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=rpoly", title = "+proj=rpoly",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Sinusoidal

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=sinu", title = "+proj=sinu",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Spilhaus

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=spilhaus", title = "+proj=spilhaus",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Times

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=times", title = "+proj=times",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Tissot Modified Conic

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=tissot +lat_1=60 +lat_2=65", title = "+proj=tissot +lat_1=60 +lat_2=65",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Transverse Mercator

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=tmerc", title = "+proj=tmerc",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Tobler–Mercator

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=tobmerc", title = "+proj=tobmerc",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Two-Point Equidistant

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=tpeqd +lat_1=60 +lat_2=65", title = "+proj=tpeqd +lat_1=60 +lat_2=65",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Urmaev V

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=urm5 +n=0.9 +alpha=2 +q=4", title = "+proj=urm5 +n=0.9 +alpha=2 +q=4",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Urmaev Flat-Polar Sinusoidal

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=urmfps +n=0.5", title = "+proj=urmfps +n=0.5",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Van der Grinten

::: tabs

== I

````@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=vandg", title = "+proj=vandg",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
````

== II

````@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=vandg2", title = "+proj=vandg2",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
````

== III

````@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=vandg3", title = "+proj=vandg3",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
````

== IV

````@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=vandg4", title = "+proj=vandg4",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
````

:::

## Vitkovsky I

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=vitk1 +lat_1=45 +lat_2=55", title = "+proj=vitk1 +lat_1=45 +lat_2=55",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Wagner

::: tabs

== I

````@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=wag1", title = "+proj=wag1",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
````

== II

````@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=wag2", title = "+proj=wag2",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
````

== III

````@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=wag3", title = "+proj=wag3",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
````

== IV

````@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=wag4", title = "+proj=wag4",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
````

== V

````@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=wag5", title = "+proj=wag5",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
````

== VI

````@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=wag6", title = "+proj=wag6",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
````

== VII

````@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=wag7", title = "+proj=wag7",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
````

:::

## Web Mercator (Pseudo-Mercator)

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=webmerc +datum=WGS84", title = "+proj=webmerc +datum=WGS84",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Werenskiold I

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=weren", title = "+proj=weren",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Winkel

::: tabs

== I

````@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=wink1", title = "+proj=wink1",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
````

== II

````@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=wink2", title = "+proj=wink2",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
````

:::

## Winkel Tripel

```@example projections
fig = Figure(size = (420, 300))
ga = GeoAxis(fig[1, 1]; dest = "+proj=wintri", title = "+proj=wintri",
    titlesize = 9, titlefont = :regular)
hidedecorations!(ga; grid = false)
poly!(ga, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
fig
```

## Polar stereographic (`GeoPolarAxis`)

!!! note "A separate axis type"
    Pole-centred polar maps use [`GeoPolarAxis`](@ref) rather than `GeoAxis`. It is a
    distinct axis type on purpose, so that a circular boundary, a polar graticule, and a
    circular spine all come for free.

Pass the cap latitude as `latcap` (its sign picks the pole: `latcap ≥ 0` north, `< 0` south),
then plot with the usual verbs using geographic `(lon, lat)` data:

```@example projections
polar = Figure(size = (840, 460))
for (i, (latcap, ttl)) in enumerate([
        (55, "North polar stereographic"),
        (-55, "South polar stereographic")])
    gpa = GeoPolarAxis(polar[1, i]; latcap = latcap, title = ttl, titlesize = 11)
    poly!(gpa, land; color = (:gray70, 0.55), strokecolor = :black, strokewidth = 0.3)
end
polar
```

For a filled field on a cap, see the [Polar stereographic cap](@ref) example.

```@setup projections
## Reset CairoMakie to PNG (the backend type is process-global) so later raster-heavy example
## pages don't balloon into huge SVGs.
CairoMakie.activate!(px_per_unit = 2, type = :png)
```
